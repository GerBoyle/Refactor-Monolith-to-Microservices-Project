version: "3"
services:
  reverseproxy:
    build:
      context: .
    image: dockerUser/reverseproxy 
  backend_user:
    build:
      context: .
    image: dockerUser/udagram-api-user
  backend_feed:
    build:
      context: . 
    image: dockerUser/udagram-api-feed
  frontend:
    build:
      context: . 
    image: dockerUser/udagram-frontend:local
