version: "3"
services:
  reverseproxy:
    build:
      context: .
    image: dockerUser/reverseproxy 
  backend_user:
    build:
      context: ../../udacity-c3-restapi-user
    image: dockerUser/udacity-restapi-user
  backend_feed:
    build:
      context: ../../udacity-c3-restapi-feed  
    image: dockerUser/udacity-restapi-feed
  frontend:
    build:
      context: ../../udacity-c3-frontend  
    image: dockerUser/udacity-frontend:local
